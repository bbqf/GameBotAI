# Implementation Plan: Reorder Spec Folders

**Branch**: `038-reorder-spec-folders` | **Date**: 2026-05-30 | **Spec**: [spec.md](spec.md)  
**Input**: Feature specification from `specs/043-reorder-spec-folders/spec.md`

## Summary

Rename all 42 existing spec folders under `specs/` to sequential numbers (001–042) based on chronological git creation order, eliminating the current duplicate `001-*` and `004-*` prefixes and the gap at 014–016. Additionally bump the installer minor version from 4 to 5. No source code is modified; all changes are confined to `specs/` folder names, spec metadata fields within those folders, `.specify/feature.json`, and `installer/versioning/version.override.json`.

## Technical Context

**Language/Version**: N/A — no new code; rename operations are filesystem-level  
**Primary Dependencies**: Git (history lookup for ordering), filesystem rename  
**Storage**: `specs/` directory (folder renames); `installer/versioning/version.override.json` (JSON update)  
**Testing**: N/A — no executable logic added  
**Target Platform**: Windows with Git Bash  
**Project Type**: Maintenance  
**Performance Goals**: N/A  
**Constraints**: No `.cs`, `.ts`, `.vue`, or runtime-consumed configuration files modified  
**Scale/Scope**: 40 folder renames (2 no-ops), 40 spec.md front-matter field updates, 1 JSON version update, 1 `.specify/feature.json` update

## Constitution Check

| Gate | Status | Notes |
|------|--------|-------|
| Code Quality (I) | ✅ PASS | No new code; linting/static analysis not applicable |
| Testing Standards (II) | ✅ PASS | No executable logic added; test suite untouched |
| UX Consistency (III) | ✅ PASS | No user-facing interface changes |
| Performance (IV) | ✅ PASS | No hot paths affected |
| Build/Test State | ✅ PASS | Repo is clean on master; all tests green per git status |

No violations. No waivers required.

## Project Structure

### Documentation (this feature)

```text
specs/043-reorder-spec-folders/
├── plan.md              ← this file
├── research.md          ← complete renaming map and ordering rationale
├── data-model.md        ← Spec Folder and Version Override entities
└── tasks.md             ← generated by /speckit-tasks
```

### Files Modified by This Feature

```text
specs/                   ← 40 folder renames (2 are no-ops per research.md)
  001-*/spec.md          ← front-matter fields updated (Feature Branch, Spec Directory)
  003-*/spec.md
  ... (all renamed folders with spec.md containing self-referential paths)

installer/versioning/
  version.override.json  ← minor: "4" → "5", updatedAtUtc updated

.specify/
  feature.json           ← feature_directory: specs/043-reorder-spec-folders
```

**Structure Decision**: Single-pass filesystem maintenance. No new source trees created. The rename ordering described in research.md (highest current number first) prevents transient conflicts.

## Implementation Phases

### Phase 1: Rename Existing Spec Folders

Execute the 40 effective renames (from `research.md` renaming map) in safe order:

**Step 1 — High-range numbered folders (037 → 042, descending):**
```
037-loop-step-management      → 042-loop-step-management
036-tap-wait-retry            → 037-tap-wait-retry
035-ui-config-editor          → 036-ui-config-editor
034-background-screenshot-service → 035-background-screenshot-service
033-command-loops             → 034-command-loops
```

**Step 2 — Tie-swap (031/032 group):**
```
031-sequence-conditional-steps → 033-sequence-conditional-steps
  (032-per-step-conditions stays as-is — no rename needed)
```

**Step 3 — Mid-range numbered folders (030 → 031, descending):**
```
030-sequence-conditional-logic → 031-sequence-conditional-logic
029-execution-logs-tab        → 030-execution-logs-tab
028-execution-log             → 029-execution-log
027-add-primitive-tap-action  → 028-add-primitive-tap-action
026-installer-semver-upgrade  → 027-installer-semver-upgrade
025-standalone-windows-installer → 026-standalone-windows-installer
024-backend-webui-installer   → 025-backend-webui-installer
023-authoring-execution-ui    → 024-authoring-execution-ui
022-emulator-image-crop       → 023-emulator-image-crop
021-images-authoring-ui       → 022-images-authoring-ui
020-connect-game-action       → 021-connect-game-action
019-web-ui-nav                → 020-web-ui-nav
018-api-refactor              → 019-api-refactor
017-unify-authoring-ui        → 018-unify-authoring-ui
013-semantic-actions-ui       → 017-semantic-actions-ui
012-web-ui-authoring          → 015-web-ui-authoring
011-sequence-logic            → 014-sequence-logic
010-command-sequences         → 013-command-sequences
009-image-detect-command      → 012-image-detect-command
008-image-match-detections    → 011-image-match-detections
007-image-storage             → 010-image-storage
006-ocr-confidence-refactor   → 009-ocr-confidence-refactor
005-fix-trigger-evaluate      → 008-fix-trigger-evaluate
004-tesseract-logging         → 006-tesseract-logging
004-logging-config-refresh    → 005-logging-config-refresh
003-command-trigger-tests     → 004-command-trigger-tests
```

**Step 4 — Late-arriving 001-* folders (targets 038–041 are free):**
```
001-sequence-random-delay     → 038-sequence-random-delay
001-primitive-actions-refactor → 039-primitive-actions-refactor
001-wait-for-image            → 040-wait-for-image
001-fix-sequence-step-names   → 041-fix-sequence-step-names
```

**Step 5 — Remaining 001-* with low targets (safe once high targets are done):**
```
001-authoring-crud-ui         → 016-authoring-crud-ui
001-runtime-logging-control   → 007-runtime-logging-control
001-action-command-refactor   → 003-action-command-refactor
```

**Step 6 — No-ops (verify unchanged, do not rename):**
```
001-save-config               ← already 001, no action
002-config-logging-hardening  ← already 002, no action
032-per-step-conditions       ← already 032, no action
043-reorder-spec-folders      ← this spec, already 043, no action
```

### Phase 2: Update Spec Metadata

For each renamed folder that contains a `spec.md` with a `Feature Branch` or `Spec Directory` front-matter field referencing the old folder name: update those fields to the new folder name.

This is a find-and-replace on the spec.md files of the affected folders only. No other file types are modified.

### Phase 3: Update Version Override

Edit `installer/versioning/version.override.json`:
- `minor`: `"4"` → `"5"`
- `updatedAtUtc`: set to current date (2026-05-30T00:00:00Z)
- All other fields unchanged

### Phase 4: Update `.specify/feature.json`

Confirm `feature_directory` is `specs/043-reorder-spec-folders` (already set during spec creation). No further change required.

### Phase 5: Verify

1. `ls specs/ | wc -l` equals 43 (42 renamed + 1 new + openapi.json = 44 total entries; 43 directories)
2. All prefixes are unique integers 001–043
3. `installer/versioning/version.override.json` shows `minor: "5"`
4. `git status` shows only the renamed files and the version JSON — no source code changes
