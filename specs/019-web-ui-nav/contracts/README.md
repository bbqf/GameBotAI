# Contracts: Web UI Navigation Restructure

No new backend APIs are introduced for this feature. The Web UI continues to use existing endpoints for Actions, Sequences, and Commands. Navigation changes are client-side only; ensure any legacy Trigger routes redirect to the Authoring area within the UI shell.
