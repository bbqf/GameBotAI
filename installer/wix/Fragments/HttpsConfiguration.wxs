<Wix xmlns="http://wixtoolset.org/schemas/v4/wxs">
  <Fragment>
    <Property Id="ENABLE_HTTPS" Value="0" />
    <Property Id="CERTIFICATE_REF" Value="__UNSET__" />
    <Property Id="HTTPS_REQUIRES_CERTIFICATE" Value="1" />

    <!-- Launch condition scaffold for T056:
         Message: HTTPS requires certificate reference.
         Expression: NOT (ENABLE_HTTPS = 1 AND CERTIFICATE_REF = "__UNSET__") -->
  </Fragment>
</Wix>
