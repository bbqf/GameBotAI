namespace GameBot.Service.Services;

internal static class DynamicDebugOptions
{
    // Toggle for optionally dumping intermediate images to disk for debugging
    public static volatile bool DumpImages;
}
